
module demo.demo_mojo.mojom;

interface Connection {

    EstablishChannel() =>
        (int32 client_id,
         handle<message_pipe>? channel_handle);

};


interface MyService {

    Hi(string message);

};

// // 子进程 2 实现，建立一个新的 pipe 并扔回去
// interface ConnectionFactory{

//     CreateGpuChannel() =>
//         (int32 client_id,
//          handle<message_pipe>? channel_handle);

// }